import { themeToolbar } from '@/themes/active';

/** Theme-aware toolbar buttons — TopNav row 3 and page toolbars. */
export const NEXOR_TOOLBAR_BTN = themeToolbar.btn;
export const NEXOR_TOOLBAR_BTN_SM = themeToolbar.btnSm;
export const NEXOR_ACTION_BTN = themeToolbar.action;
export const NEXOR_TAB_TRIGGER = themeToolbar.tab;

/** Polished pill buttons — dashboard shortcuts, ledger exports, feature actions. */
export const NEXOR_PILL_BTN = themeToolbar.pill;
export const NEXOR_PILL_BTN_PRIMARY = themeToolbar.pillPrimary;

/** Large feature shortcut — dashboard BI sidebar (chart of accounts / SAF-T, etc.). */
export const NEXOR_FEATURE_SHORTCUT_BTN = themeToolbar.feature;
