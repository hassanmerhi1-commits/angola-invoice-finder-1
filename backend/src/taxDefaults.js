/** Default IVA/VAT rate for new products and document lines when none is specified. */
const DEFAULT_VAT_RATE = 5;
const DEFAULT_TAX_CODE = 'IVA5';

module.exports = { DEFAULT_VAT_RATE, DEFAULT_TAX_CODE };
